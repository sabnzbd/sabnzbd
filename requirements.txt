six
sabyenc3
cheetah3
cryptography
feedparser
configobj
cherrypy
portend
chardet
gntp

# Not on Windows
pygobject>=3.10.2; sys_platform != 'win32'

# Only on Windows
pywin32>=227; sys_platform == 'win32'

# Only on macOS
pyobjc; sys_platform == 'darwin'
